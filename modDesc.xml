<?xml version="1.0" encoding="utf-8" standalone="no"?>
<modDesc descVersion="109">
    <author>Nalathan</author>
    <version>1.0.0.0</version>
    <title>
        <en>Server Player Count</en>
        <de>Server Spieleranzeige</de>
    </title>
    <description>
        <en><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></en>
        <de><![CDATA[Zeigt auf Multiplayer-Servern die aktuelle Spieleranzahl direkt im HUD an.

Die Anzeige besteht aus einer kompakten Spielerzahl mit Server-Statuspunkt:
Grün: keine serverseitige Lua-Warning oder kein serverseitiger Lua-Error erkannt
Gelb: serverseitige Lua-Warning erkannt
Rot: serverseitiger Lua-Error erkannt

Der Server-Statuspunkt kann in den Spieleinstellungen zurückgesetzt werden, falls eine erkannte Meldung geprüft wurde und unkritisch ist.

Hinweis: Im Singleplayer wird die HUD-Anzeige automatisch ausgeblendet, falls die Mod dort versehentlich aktiviert wurde.

Weitere Erkennungen können später ergänzt werden, falls Logdaten im Spiel sauberer ausgewertet werden können.]]></de>
        <fr><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></fr>
        <es><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></es>
        <it><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></it>
        <nl><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></nl>
        <pl><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></pl>
        <cz><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></cz>
        <cs><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></cs>
        <hu><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></hu>
        <ru><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></ru>
        <pt><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></pt>
        <br><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></br>
        <tr><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></tr>
        <ro><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></ro>
        <jp><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></jp>
        <kr><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></kr>
        <ct><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></ct>
        <ea><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></ea>
        <da><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></da>
        <fi><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></fi>
        <no><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></no>
        <sv><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></sv>
        <fc><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></fc>
        <uk><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></uk>
        <vi><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></vi>
        <id><![CDATA[Shows the current player count directly in the HUD on multiplayer servers.

The display consists of a compact player count with a server status indicator:
Green: no server-side Lua warning or server-side Lua error detected
Yellow: server-side Lua warning detected
Red: server-side Lua error detected

The server status indicator can be reset in the game settings if a detected message has been checked and is not critical.

Note: In singleplayer, the HUD display is hidden automatically if the mod was enabled there by mistake.

Additional detections may be added later if log data can be evaluated more cleanly in-game.]]></id>
    </description>
    <iconFilename>icon_ServerPlayerCount.dds</iconFilename>
    <multiplayer supported="true"/>
    <extraSourceFiles>
        <sourceFile filename="scripts/ServerPlayerCount.lua"/>
    </extraSourceFiles>
    <l10n>
        <text name="spc_hudLabel">
            <en>Players online</en>
            <de>Spieler online</de>
        </text>
        <text name="spc_playersOnline">
            <en>Players online</en>
            <de>Spieler online</de>
        </text>
        <text name="spc_settings_header">
            <en>Server Player Count</en>
            <de>Server Spieleranzeige</de>
        </text>
        <text name="spc_settings_resetStatus_label">
            <en>Warning Status</en>
            <de>Warnstatus</de>
        </text>
        <text name="spc_settings_resetStatus_button">
            <en>Reset Status</en>
            <de>Status zurücksetzen</de>
        </text>
    </l10n>
</modDesc>
